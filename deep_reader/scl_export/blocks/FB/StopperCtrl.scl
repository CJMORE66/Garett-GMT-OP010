FUNCTION_BLOCK "StopperCtrl"
VAR_INPUT
  i_SysRun : Bool;
  i_AutoMode : Bool;
  i_AutoRunnig : Bool;
  i_Reset : Bool;
  i_LineRunning : Bool;
  i_PalletArrivedNextStation : Bool;
  i_StopperSensor : Bool;
  "i_Cyl-Home" : Bool;
  i_NextStationReady : Bool;
  i_PresentDelayTime : Time;
  i_ReleaseDelayTime : Time;
END_VAR
VAR_OUTPUT
  o_StopperClyWP : Bool;
  o_StopperClyHP : Bool;
END_VAR
VAR_IN_OUT
  io_NextyStopperStatus : Int;
  io_Interface : "UDT_StopperInterface";
  io_MotorCWRun : Bool;
  io_MotorCCWRun : Bool;
END_VAR
VAR
  s_Sequence : Int;
  s_SequenceEnable : Bool;
  s_SequenceFirstEnable : Bool;
  s_StopperSensorOnDelay : TON_TIME;
  s_StopperSensorOffDelay : TON_TIME;
  s_PalletMissingConfirmDelay : TON_TIME;
  s_PalletToNextStationDelay : TON_TIME;
  s_SequenceFirstAutoDelay : TON_TIME;
  s_SequenceFirstAuto : Int;
  s_StopperSensor : Bool;
  s_PalletArrivedNextStation : Bool;
  P : Array[0..10] of Bool;
END_VAR
VAR_TEMP
  t_AlwaysOn : Bool;
END_VAR
BEGIN
  // Původní jazyk: LAD
  // Zdroj XML: EXPORT/Program blocks/OP010/10_Device/26_Stopper/01_FuncationBlock/StopperCtrl.xml
  // --- LAD/FBD → SCL (orientační převod, jen pro revizi) ---
  // Network 1
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=27: nelze určit podmínku (vstupní síť).
  // Network 2
  #s_SequenceEnable := (((((#t_AlwaysOn AND #i_SysRun) AND #i_AutoRunnig) AND #i_AutoMode) AND NOT #io_Interface.ArrNextError) AND NOT #io_Interface.MissingError);
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(1)
  // RCoil UId=43: nelze určit podmínku (vstupní síť).
  // RCoil UId=44: nelze určit podmínku (vstupní síť).
  // RCoil UId=45: nelze určit podmínku (vstupní síť).
  // Network 3
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(2), Eq(1), TON(1)
  // RCoil UId=43: nelze určit podmínku (vstupní síť).
  // RCoil UId=44: nelze určit podmínku (vstupní síť).
  // Coil UId=54: nelze určit podmínku (vstupní síť).
  // Network 4
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(4), Move(2), TON(1)
  // Coil UId=45: nelze určit podmínku (vstupní síť).
  // Network 5
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(1), Move(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 6
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(3), Eq(2), TON(1), PBox(1)
  // SCoil UId=43: nelze určit podmínku (vstupní síť).
  // RCoil UId=44: nelze určit podmínku (vstupní síť).
  // SCoil UId=45: nelze určit podmínku (vstupní síť).
  // RCoil UId=55: nelze určit podmínku (vstupní síť).
  // SCoil UId=56: nelze určit podmínku (vstupní síť).
  // Network 7
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(1), Move(1), TON(1)
  // Coil UId=41: nelze určit podmínku (vstupní síť).
  // RCoil UId=42: nelze určit podmínku (vstupní síť).
  // SCoil UId=43: nelze určit podmínku (vstupní síť).
  // RCoil UId=44: nelze určit podmínku (vstupní síť).
  // SCoil UId=51: nelze určit podmínku (vstupní síť).
END_FUNCTION_BLOCK
