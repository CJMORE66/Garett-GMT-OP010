FUNCTION_BLOCK "MES_CHeck"
VAR
  Station_1 : "MES Check_fb";
  Station_2 : "MES Check_fb";
  Station_3 : "MES Check_fb";
  Station_4 : "MES Check_fb";
  Station_5 : "MES Check_fb";
  Station_6 : "MES Check_fb";
  Station_7 : "MES Check_fb";
  Station_8 : "MES Check_fb";
END_VAR
BEGIN
  // Původní jazyk: LAD
  // Zdroj XML: EXPORT/Program blocks/OP010/11_MES/MES_CHeck.xml
  // --- LAD/FBD → SCL (orientační převod, jen pro revizi) ---
  // Network 1
  // Network 2
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(2)
  // Coil UId=30: nelze určit podmínku (vstupní síť).
  // Coil UId=32: nelze určit podmínku (vstupní síť).
  // Network 3
  // Network 4
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(2)
  // Coil UId=30: nelze určit podmínku (vstupní síť).
  // Coil UId=32: nelze určit podmínku (vstupní síť).
  // Network 5
  // Network 6
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(2)
  // Coil UId=30: nelze určit podmínku (vstupní síť).
  // Coil UId=32: nelze určit podmínku (vstupní síť).
  // Network 7
  // Network 8
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Eq(2)
  // Coil UId=30: nelze určit podmínku (vstupní síť).
  // Coil UId=32: nelze určit podmínku (vstupní síť).
END_FUNCTION_BLOCK
