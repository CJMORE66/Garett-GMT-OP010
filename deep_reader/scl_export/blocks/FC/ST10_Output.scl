FUNCTION "ST10_Output" : Void
BEGIN
  // Původní jazyk: LAD
  // Zdroj XML: EXPORT/Program blocks/OP010/02_Output/ST10_Output.xml
  // --- Rekonstruovaný StructuredText (best-effort) ---
  // Network 1
  oSafetyDoorLocked_1 := .[];
  oSafetyDoorLocked_2 := .[];
  oSafetyDoorLocked_3 := .[];
  oSafetyDoorLocked_4 := .[];
  oSafetyDoorLocked_5 := .[];
  oSafetyDoorLocked_6 := .[];
  oSafetyDoorLocked_7 := .[];
  oSafetyDoorLocked_8 := .[];
  oSafetyDoorLocked := .[];
  // Network 2
  oMaterielDoorLocked_H1 := .[];
  oMaterielDoorLocked_H2 := .[];
  oMaterielDoorLocked_J1 := .[];
  oMaterielDoorLocked_J2 := .[];
  oMaterielDoorLocked_K1 := .[];
  oMaterielDoorLocked_K2 := .[];
  oMaterielDoorLocked_L1 := .[];
  oE-NG-01SafetyGate := .[];
  oE-NG-02SafetyGate := .[];
  // --- LAD/FBD → SCL (orientační převod, jen pro revizi) ---
  // Network 1
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Network 2
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 3
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=31: nelze určit podmínku (vstupní síť).
  // Network 4
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=43: nelze určit podmínku (vstupní síť).
  // Network 5
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): PBox(2), O(1)
  // SCoil UId=58: nelze určit podmínku (vstupní síť).
  // SCoil UId=59: nelze určit podmínku (vstupní síť).
  // SCoil UId=60: nelze určit podmínku (vstupní síť).
  // SCoil UId=61: nelze určit podmínku (vstupní síť).
  // SCoil UId=62: nelze určit podmínku (vstupní síť).
  // SCoil UId=63: nelze určit podmínku (vstupní síť).
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // SCoil UId=65: nelze určit podmínku (vstupní síť).
  // SCoil UId=66: nelze určit podmínku (vstupní síť).
  // RCoil UId=69: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // RCoil UId=71: nelze určit podmínku (vstupní síť).
  // RCoil UId=72: nelze určit podmínku (vstupní síť).
  // RCoil UId=73: nelze určit podmínku (vstupní síť).
  // RCoil UId=74: nelze určit podmínku (vstupní síť).
  // RCoil UId=75: nelze určit podmínku (vstupní síť).
  // RCoil UId=76: nelze určit podmínku (vstupní síť).
  // RCoil UId=77: nelze určit podmínku (vstupní síť).
  // Network 6
  ST10_SafetyGate_InterfaceDB.aDev := ((((((((Sys.COM.AlwaysON AND oSafetyDoorLocked_1) AND oSafetyDoorLocked_2) AND oSafetyDoorLocked_3) AND oSafetyDoorLocked_4) AND oSafetyDoorLocked_5) AND oSafetyDoorLocked_6) AND oSafetyDoorLocked_7) AND oSafetyDoorLocked_8);
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): PBox(2), Move(2)
  // SCoil UId=74: nelze určit podmínku (vstupní síť).
  // SCoil UId=76: nelze určit podmínku (vstupní síť).
  // SCoil UId=78: nelze určit podmínku (vstupní síť).
  // SCoil UId=80: nelze určit podmínku (vstupní síť).
  // SCoil UId=82: nelze určit podmínku (vstupní síť).
  // SCoil UId=84: nelze určit podmínku (vstupní síť).
  // SCoil UId=86: nelze určit podmínku (vstupní síť).
  // SCoil UId=88: nelze určit podmínku (vstupní síť).
  // SCoil UId=90: nelze určit podmínku (vstupní síť).
  // RCoil UId=93: nelze určit podmínku (vstupní síť).
  // RCoil UId=94: nelze určit podmínku (vstupní síť).
  // RCoil UId=95: nelze určit podmínku (vstupní síť).
  // RCoil UId=96: nelze určit podmínku (vstupní síť).
  // RCoil UId=97: nelze určit podmínku (vstupní síť).
  // RCoil UId=98: nelze určit podmínku (vstupní síť).
  // RCoil UId=99: nelze určit podmínku (vstupní síť).
  // RCoil UId=100: nelze určit podmínku (vstupní síť).
  // RCoil UId=101: nelze určit podmínku (vstupní síť).
  // Coil UId=111: nelze určit podmínku (vstupní síť).
  // Network 7
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(2)
  // Coil UId=40: nelze určit podmínku (vstupní síť).
  // Network 8
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(2)
  // Coil UId=40: nelze určit podmínku (vstupní síť).
  // Network 9
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 10
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 11
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 12
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 13
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 14
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 15
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 16
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 17
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 18
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // SCoil UId=70: nelze určit podmínku (vstupní síť).
  // RCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 19
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 20
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 21
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 22
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 23
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 24
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 25
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 26
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
  // Network 27
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): O(1)
  // Coil UId=33: nelze určit podmínku (vstupní síť).
  // Network 28
  // --- Poznámky / omezení převodu ---
  // Síť obsahuje nepodporované prvky (ponecháno jen jako komentář): Move(5), Eq(4), Ne(4), PBox(2)
  // SCoil UId=64: nelze určit podmínku (vstupní síť).
  // RCoil UId=65: nelze určit podmínku (vstupní síť).
  // RCoil UId=70: nelze určit podmínku (vstupní síť).
  // SCoil UId=71: nelze určit podmínku (vstupní síť).
END_FUNCTION
