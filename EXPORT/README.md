# OP10 analysis deliverables v2

Includes:
- Inventory, call graph, legacy interface map, GRAPH step map
- Additional sequencer spec + SCL template for Next/Prev/Goto via SafeBaseline

NOTE:
- Interface extraction reports symbol usage (read/write direction not reliably derivable from FlgNet alone).
